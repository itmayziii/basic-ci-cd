run:
  modules-download-mode: 'readonly'
  timeout: 5m
output:
  formats:
    - format: 'line-number'
      path: stdout
linters:
  disable-all: true
  enable:
    - 'errcheck'
    - 'gosimple'
    - 'govet'
    - 'ineffassign'
    - 'staticcheck'
    - 'unused'
    - 'gofmt'
